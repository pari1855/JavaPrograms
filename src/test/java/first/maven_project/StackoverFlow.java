package first.maven_project;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class StackoverFlow {
	 /*   String name = "";
	    String username = "";
	    String passWord = "";
	    
	    	
	 public void login(String driver) throws FileNotFoundException {

	        File file = new File("src/main/resources/UserNames/userName.txt");
	        Scanner scan = new Scanner(file);

	        // test first line
	        while (scan.hasNextLine()) {
	            name = scan.nextLine();
	            if (name.equalsIgnoreCase("Name:KYE-APP")) { 
	           // name change to select the correct user and password

	                username = scan.nextLine();
	                System.out.println(username);
	                //WebElement userName = driver.findElement(By.xpath("//input[@type='email']"));
	                //userName.sendKeys(username);

	                passWord = scan.nextLine();
	                System.out.println(passWord);
	                //WebElement password = driver.findElement(By.xpath("//input[@type='password']"));
	                //password.sendKeys(passWord);

}
	        //WebElement Submitbutton = driver.findElement(By.xpath("//button[@type='submit']"));
	        //Submitbutton.click();
	        	
	    }
	 }
	 
	 public static void main(String[] args) throws FileNotFoundException {
		 	String driver;
		 	StackoverFlow obj = new StackoverFlow();
		 	obj.login("abc");
		 	
	    }*/
	public static void main(String[] args) {
	 int[] arr = new int[0];
	 System.out.println(arr[-1]);
	}
}